apiVersion: v1
kind: Service
metadata:
  name: order
  labels:
    service: order
    project: snackbar
  namespace: snackbar
spec:
  ports:
    - port: 80
      targetPort: 8080
  selector:
    service: order
    project: snackbar
  type: NodePort
