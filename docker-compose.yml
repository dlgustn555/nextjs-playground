version: "3.9"

services:
  nginx:
    image: nextjs-nginx
    restart: unless-stopped
    ports:
      - 80:80

  nextjs:
    image: nextjs-app
    restart: unless-stopped
    ports:
      - 3000:3000
